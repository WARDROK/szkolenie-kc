# Production API endpoint
# Use /api if frontend and backend are served from the same domain (e.g. via reverse proxy)
# Use an absolute URL if they are on separate domains:
#   VITE_API_URL=https://myapp.azurewebsites.net/api
VITE_API_URL=/api
